# 发布检查清单

这份文件用于每次修改后的回归检查，重点覆盖容易出错的 UI、垫抽数和周期奖励逻辑。

## 使用规则

- 每次修改代码后，先运行 `./build-standalone-app.sh`。
- 涉及 UI 的修改必须手动检查窗口、弹窗、滚动、输入框和点击区域，不只看编译结果。
- 涉及奖励或日期的修改必须检查领取、取消、历史记录和重新打开后的状态。
- 涉及垫抽数的修改必须检查同类卡池同步、不同卡池隔离、输入提交和持久化。
- 每次检查后在文末新增一条日期记录，写明检查范围、结果和未解决问题。

## 编译与启动

- [ ] `./build-standalone-app.sh` 编译成功。
- [ ] Xcode 中可选择 `PTNHypercubeWidgetiOS` target，并在 iPhone 模拟器或真机编译。
- [ ] iOS target 已启用 `iCloud.com.openai.PTNHypercubeWidget` CloudKit 容器。
- [ ] Mac 与 iPhone 登录同一 Apple ID 后，库存、领取记录、历史和抽卡规划可互相同步。
- [ ] `dist/PTNHypercubeWidget.app` 成功打开。
- [ ] 应用启动后没有崩溃、白屏或空窗口。
- [ ] 状态栏 M 图标正常显示，菜单可以显示、隐藏、切换透明度和退出。

## 窗口与整体 UI

- [ ] 小组件只出现在桌面层，不遮挡或穿透触发后面的应用内容。
- [ ] 窗口可以移动，重新打开后保留上次位置。
- [ ] 窗口圆角、毛玻璃、透明度和边框没有出现多余透明边。
- [ ] 顶部库存区域固定不滚动，下面的奖励区域可以滚动。
- [ ] 当前周期和抽卡规划分别滚动，并分别记住滚动位置。
- [ ] 常驻奖励与当前周期、抽卡规划平级显示，并单独记住滚动位置。
- [ ] 常驻奖励不按领取状态沉底，派遣和审查固定排在前两项。
- [ ] 点击小组件内部不会穿透到后面的应用或组件。
- [ ] 所有页面领取后保持原位置，只更新勾选、文字颜色和库存。

## 顶部库存与 UP 弹窗

- [ ] 总抽数使用异方晶折合值向下取整，红票不计入总抽数。
- [ ] 蓝票、红票和异方晶数值与编辑库存后的结果一致。
- [ ] UP 数与未完成的计划卡池一致，已抽完项目不计入 UP 数。
- [ ] UP 尖号位置稳定，弹窗左上角保持当前初始偏移，不被后方文字覆盖。
- [ ] UP 弹窗包含活动池、复刻池、统合池和限定池，并显示对应角色或枷锁数量。
- [ ] 点击尖号展开，再次点击或点击其他位置收起。
- [ ] UP 弹窗展开后不会改变主界面布局或滚动位置。

## 垫抽数与抽卡规划

- [ ] 垫抽数输入框未编辑时无多余背景，编辑时输入内容清晰可见。
- [ ] 垫抽数支持数字输入，输入完成后数值不会与光标或默认值重叠。
- [ ] 回车或点击其他位置后，垫抽数成功提交并结束编辑状态。
- [ ] 活动池和复刻池分别保存垫抽数，互不覆盖；各自连续卡池继承本池数值。
- [ ] 统合池、定轨池和限定池各自独立，互不覆盖垫抽数。
- [ ] 切换当前周期和抽卡规划后，垫抽数仍保持正确。
- [ ] 重新打开应用后，垫抽数、角色选择、枷锁数量和抽卡状态仍然存在。

## 2026-08-18 倒计时校准回归

- [x] 基地监管按 3 小时 50 分 24 秒生产 4 异方晶。
- [x] 满仓倒计时按 48 异方晶容量显示 `HH:MM:SS`。
- [x] 凌晨锚点和 42 秒校准迁移不重复发放历史奖励。
- [x] `git diff --check` 和独立构建通过，最新 App 已启动。

## 2026-08-21 奖励数值与更新维护

- [x] 数据间隙、派遣和更新维护数值与领取入口完成配置检查。
- [x] 模块弹窗统一使用兑换码弹窗的根层自适应定位，卡池弹窗不会改变卡片布局。

## 2026-08-18 数据间隙六段奖励

- [x] 六个奖励按圆形、椭圆交替显示，奖励值为 150、340、160、340、110、340。
- [x] 第一、二项 15:00 解锁，第三、四项次日 05:00 解锁，第五、六项第三日 05:00 解锁。
- [x] 领取、取消、历史记录和库存继续复用每日进度逻辑。
- [x] `git diff --check` 和独立构建通过，最新 App 已启动。
- [ ] 卡池开始前、进行中和结束后的状态、时间显示及排序正确。

## 周期奖励与日期逻辑

- [ ] 每日监察任务按奖励日刷新；基地监管按固定生产周期自动增加 4 异方晶。
- [ ] 周监察任务按周一周期显示 20、20、20、40、50 五个可独立领取的圆点。
- [ ] 每日奖励按每天 04:00 更新，周奖励按每周一 04:00 更新。
- [ ] 周监察任务、每周分享和商店兑换的奖励数值及周期正确。
- [ ] 监管事件每天显示一次并按每日周期刷新，奖励为 20 异方晶。
- [ ] 情绪检测按配置的月份和日期显示，月度状态可以正确重置。
- [ ] 暗域只在周一出现，严格使用真实日期和六周赛季周期。
- [ ] 暗域第31期以 2026-08-10 为第1周，2026-08-31 正确显示第4周，并保留已领取的赛季奖励。
- [ ] 暗域普通周奖励为 510 异方晶，新赛季首周包含 450 异方晶赛季奖励。
- [ ] 数据间隙只显示已配置日期，不推测未知日期或自动补全后续周期。
- [ ] 时间未知来源可以手动领取、取消和切换记录周期。

## 密令、试用与额外记录

- [ ] N9、N10 和核心危机模块固定显示，圆点领取后永久保留且不自动刷新。
- [ ] 监察密令普通模式显示六个奖励圆点，高级模式额外显示两个高级异方晶奖励圆点。
- [ ] 高级监察密令开关可以在普通奖励和高级奖励之间切换，普通奖励保持 `x/6` 的旧领取键。
- [ ] 普通奖励、额外记录、密令、派遣、审查和卡池主勾选圆圈统一为 18pt 直径和一致描边。
- [ ] 普通奖励与额外记录卡片边框同宽，圆圈使用同一 24pt 对齐槽位，标题和奖励数值基线一致。
- [ ] 额外记录普通条目以暗域奖励行为基准，使用相同填充、圆角、间距和文字层级。
- [ ] 额外记录区块不再额外横向缩进，卡片左右边框与暗域奖励行完全对齐。
- [ ] 密令领取前后保持固定位置，不因完成状态改变顺序。
- [ ] 全部倒计时与配置的结束时间一致，超过一天只显示天数，不足一天且至少一小时向上取整显示小时，不足一小时显示分钟。
- [ ] 监察密令、活动小游戏和兑换码全部完成后，仅文字颜色变暗，卡片和圆点保持原样。
- [ ] 新角色试用奖励的显示和数值与当前活动池配置一致。
- [ ] 活动小游戏五个圆点各自对应 50 异方晶，支持领取、取消和手动刷新周期。
- [ ] 兑换码位于常驻奖励，连续活动池显示 3 个 `+200 晶` 圆点，单个活动池显示 1 个圆点，并按活动池周期自动更新。
- [ ] 派遣以监管事件式单行显示，前置 15、20、40 异方晶三个同尺寸圆点，点击分别记录对应奖励，右侧实时显示累计值并通过手动刷新开启新周期。
- [ ] 派遣和审查圆圈视觉尺寸与监察密令一致，外部点击区域保持完整可用。
- [ ] 派遣和审查圆圈按监察密令的间距排列。
- [ ] 派遣已领取圆圈使用监察密令一致的粉色实心白字状态，未领取保持空心。
- [ ] 审查橙色四阶段奖励依次为 80、80、50、80 异方晶，紫色三阶段奖励依次为 60、50、60 异方晶。
- [ ] 审查显示 4 个橙色和 3 个紫色独立圆圈，按阶段顺序点击并沿用原领取键。
- [ ] 审查每个阶段圆点可独立领取或取消，整组归零只通过手动刷新。
- [ ] 额外记录中的项目领取后保持原位置，不按完成状态重新排序。

## 常驻奖励

- [ ] 派遣和审查只在常驻奖励页面显示，不再出现在额外记录。
- [ ] 停服维护按活动池显示动态标题，问卷调查按活动池显示动态标题，奖励分别为 `+200 晶` 和 `+180 晶`。
- [ ] 常驻奖励按当前活动池开始时间生成，Isomer 结束后自动切换到 Chengxiao、Phanuel 等全部后续活动池。
- [ ] 问卷调查按 Isomer 开始后第 7 天 12:00 开始，第 13 天 11:59 结束。
- [ ] 有效期结束后常驻自动奖励无论是否领取都消失，并显示结束倒计时。
- [ ] 问题修复不跟随卡池自动更新，使用右侧手动更新按钮切换周期。
- [ ] 问卷调查·随机显示 `+180 晶`，使用右侧手动更新按钮切换周期。
- [ ] 常驻奖励领取状态、库存变化、历史记录和撤销流程正确。

## 历史与持久化

- [ ] 每次领取都会写入日期时间、来源和奖励数值。
- [ ] 取消领取会扣回对应库存，并恢复对应项目的未领取状态。
- [ ] 撤销最近一条记录后，库存、项目状态和历史列表同步恢复。
- [ ] 关闭并重新打开应用后，库存、领取状态、历史、垫抽数和卡池选择仍然存在。
- [ ] 重复点击同一奖励不会重复增加库存或写入重复记录。
- [ ] 抽卡记录的赠送票计入该池抽数但不扣库存，蓝票消耗先扣蓝票、不足部分按 180 异方晶折算。
- [ ] 抽卡记录即使只有赠送票也会写入历史；撤销后恢复库存并清除对应卡池记录。
- [ ] 抽卡记录可用量包含现有蓝票、可折算异方晶和该卡池此前已扣库存，重新编辑不会重复扣除。
- [ ] 消耗弹窗的留影兑换每项领取扣除 150 异方晶，手动刷新后开启新轮次，取消和历史撤销可恢复库存。
- [ ] 增加弹窗可填写记录项和异方晶数量，确认后写入历史并增加库存。
- [ ] 历史记录统计按德国时间显示自然周和自然月净收入总结，增加与消耗合并并按 180 异方晶折算。

## 发布前检查

- [ ] `git diff --check` 通过。
- [ ] 代码修改范围与本次需求一致，没有误删已完成功能。
- [ ] 更新日志只记录面向用户的新功能或优化，不记录纯位置、偏移和实现细节。
- [ ] 工作区没有未确认的临时文件、构建产物或调试代码。
- [ ] 推送后本地分支与 `origin/main` 一致。

## 检查记录

### 2026-08-24 历史收入统计

- 范围：历史记录同级统计入口、自然周/自然月收入计算和蓝票折算。
- 结果：合并已有历史中的正向与负向异方晶及蓝票折算值，显示周期净收入。
- 未解决：无已知代码问题。

### 2026-08-22 消耗与增加

- 范围：历史右侧的消耗/增加入口、留影兑换三段奖励、手动异方晶记录和历史回滚。
- 结果：`git diff --check` 通过，独立构建通过并已重新打开最新应用。
- 手动检查：已复用现有分段奖励、历史记录和库存变更逻辑；窗口内点击与撤销待实际操作确认。
- 未解决：无已知代码问题。

### 2026-08-22 审查阶段圆圈

- 范围：审查阶段圆圈展示和按顺序点击映射。
- 结果：保留原有领取键、奖励值、取消和刷新逻辑，仅将狂级/危级阶段分别展开为 4/3 个独立圆圈。
- 未解决：无已知代码问题。

### 2026-08-14

- 范围：新增检查清单文件。
- 结果：`git diff --check` 通过，`./build-standalone-app.sh` 编译通过并成功打开应用。
- 手动检查：完整 UI 回归检查待下次涉及界面或交互修改时执行。
- 未解决：无。

### 2026-08-14 代码整理

- 范围：拆分输入框、滚动容器和窗口拖动支持组件，清理未使用成员和失效工程引用。
- 结果：`./build-standalone-app.sh` 编译通过并成功打开应用，静态检查未发现残留死代码引用。
- 手动检查：未改变 UI 参数和业务逻辑，完整 UI 回归检查待使用完整 Xcode 后执行。
- 环境限制：当前系统只有 Command Line Tools，无法执行 `xcodebuild` 工程构建。
- 未解决：无代码问题。

### 2026-08-14 二次整理检查

- 范围：复查重复结构、死代码、工程引用和发布前状态。
- 结果：未发现适合继续删除的安全代码，`git diff --check` 通过，独立构建通过并成功打开应用。
- 决定：保留现有奖励逻辑、垫抽数输入和锁定 UI，不进行高风险压缩。
- 未解决：完整 Xcode 构建仍受当前环境缺少 Xcode 限制。

### 2026-08-14 每日额外记录

- 范围：监管事件、派遣和审查的每日奖励模块。
- 结果：数据模型、每日 claim key、完成奖励和额外记录 UI 已接入，独立构建通过。
- 手动检查：圆点颜色、领取排序和撤销流程待实际操作确认。
- 未解决：无已知代码问题。

### 2026-08-14 奖励行对齐检查

- 范围：当前周期与额外记录的卡片边框、圆圈槽位、标题和奖励数值对齐。
- 结果：奖励行统一撑满内容宽度，圆圈统一使用 24pt 对齐槽位，视觉圆圈保持 18pt。
- 未解决：待独立构建及实际窗口截图确认。

### 2026-08-14 额外记录标准行检查

- 范围：额外记录中的监管事件、情绪检测和其他普通手动记录条目。
- 结果：普通条目统一采用暗域奖励行的已领取/未领取填充、圆角、间距和文字层级。
- 未解决：无已知代码问题。

### 2026-08-14 额外记录边框对齐修复

- 范围：暗域奖励行与额外记录普通条目的左右边框基准。
- 结果：移除额外记录区块的独立横向缩进，保持卡片内部圆圈和文字对齐不变。
- 未解决：待独立构建及实际窗口截图确认。

### 2026-08-14 发布前代码整理

- 范围：每日派遣/审查模型、领取状态和主界面奖励行。
- 结果：移除不再使用的整组完成奖励字段，简化重复分支；未改变现有奖励、持久化和 UI 行为。`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 常驻奖励页面

- 范围：常驻奖励页面、派遣/审查迁移、维护/问卷/问题修复奖励和动态时间规则。
- 结果：独立构建通过；常驻奖励、当前周期和抽卡规划拥有独立页面与滚动位置。
- 未解决：动态规则需在后续实际更新卡池日期时继续核对。

### 2026-08-14 常驻奖励有效期

- 范围：维护/问卷奖励结束倒计时、过期隐藏和问题修复手动更新。
- 结果：常驻自动奖励按有效窗口显示，过期后不论领取状态都隐藏；问题修复改为手动周期。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 问卷调查·随机

- 范围：常驻奖励新增问卷调查·随机手动周期项。
- 结果：奖励为 180 异方晶，支持手动更新、领取、取消、沉底和历史恢复；额外记录不重复显示。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 常驻奖励活动池联动

- 范围：维护和问卷调查·新版本改为跟随当前活动池自动更新。
- 结果：当前 Isomer 结束后整组隐藏，后续 Chengxiao 等活动池开始后自动生成新周期和结束倒计时。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 停服维护有效期

- 范围：修正停服维护补偿的结束时间。
- 结果：改为活动池开始后 4 小时出现，持续 7×24 小时后结束，并继续按活动池周期自动更新。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 常驻奖励排序

- 范围：常驻奖励固定排序及派遣、审查位置。
- 结果：常驻奖励不再因领取状态沉底，派遣和审查固定位于前两项。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 倒计时显示

- 范围：常驻奖励和密令倒计时文字格式。
- 结果：超过一天显示天数，不足一天显示小时和分钟；常驻奖励移除“结束”前缀。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 停服维护邮件有效期

- 范围：根据邮件截图修正停服维护补偿有效期。
- 结果：停服维护从活动池开始后显示，持续 7 天后结束并自动隐藏。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 倒计时天数显示

- 范围：密令、数据间隙、问卷调查和常驻奖励倒计时。
- 结果：超过一天只显示天数，不再显示附加小时；不足一天显示小时和分钟。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 停服维护倒计时边界

- 范围：停服维护邮件有效期与活动池日期关系。
- 结果：活动池开始后显示，活动池开始日后的第 7 天 00:00 结束。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 停服维护独立有效期

- 范围：修正停服维护邮件与活动池开始时刻的关系。
- 结果：活动池开始后 5 小时出现，持续 7×24 小时后在同一时刻结束。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 停服维护时间微调

- 范围：停服维护出现时间后移 1 小时。
- 结果：活动池开始后 5 小时出现，结束时间同步后移 1 小时。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 常驻奖励后续活动池继承

- 范围：确认常驻奖励对 Chengxiao 及后续活动池的自动继承。
- 结果：按活动池类型和实际时间动态选择，不依赖单个角色或固定活动池 ID。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 常驻奖励动态标题

- 范围：停服维护和问卷调查标题跟随活动池名称更新。
- 结果：当前显示主线双狂，后续自动显示 Chengxiao、Phanuel 等活动池名称。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 兑换码奖励

- 范围：兑换码自动周期、连续活动池圆点数量和领取持久化。
- 结果：主线双狂显示 3 个 200 异方晶圆点，单活动池显示 1 个；后续活动池自动换期且不继承上一期勾选。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 兑换码页面归类

- 范围：兑换码模块页面归类和固定顺序。
- 结果：兑换码移动到常驻奖励，位于派遣、审查之后；额外记录不再重复显示。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 多圆点文字完成态

- 范围：监察密令、活动小游戏和兑换码全部完成后的文字颜色。
- 结果：仅完成项目文字变暗，卡片、圆点、大小和布局保持不变。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 多圆点完成态颜色统一

- 范围：完成的监察密令、活动小游戏和兑换码文字颜色。
- 结果：统一使用数据间隙完成态的 `WidgetPalette.claimed` 颜色。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 全局固定顺序

- 范围：当前周期、额外记录和常驻奖励的领取后排序。
- 结果：移除所有沉底逻辑，领取后保持原有模块顺序，仅更新状态、颜色和库存。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 审查与派遣交互调整

- 范围：每日派遣和审查模块的显示与领取交互。
- 结果：审查合并为双色双圆点计数，派遣改为监管事件式单行显示，独立构建通过。

### 2026-08-14 圆圈与密令显示检查

- 范围：监察密令进度显示、普通奖励、额外记录、派遣、审查和卡池主勾选圆圈。
- 结果：密令恢复基础 `x/6` 显示，主界面圆圈统一为 18pt 直径和 1.5pt 描边；点击区域与布局保持不变。`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 手动检查：应用已重新打开，实际视觉效果需在当前桌面窗口中确认。
- 未解决：无已知代码问题。
- 手动检查：多次点击、完成按钮和每日刷新后的状态待实际操作确认。
- 未解决：无已知代码问题。

### 2026-08-14 派遣与审查点击修复

- 范围：派遣圆点数值显示、派遣汇总文字和审查圆点点击区域。
- 结果：派遣显示 5、15、25 并分别记录奖励，移除 +45 晶汇总，审查圆点补充明确命中区域。
- 手动检查：派遣三项及审查双色圆点的点击、库存变化和历史记录待实际操作确认。
- 未解决：无已知代码问题。

### 2026-08-14 模块配置结构整理

- 范围：奖励进度模块的类型、ID、标题、圆点数量、单格奖励和刷新能力。
- 结果：统一使用 `ProgressModuleDefinition` 与 `ProgressModuleKind`，新增模块可集中配置并由类型分流，保留旧常量和存档键兼容性。
- 结果：活动池、手动来源和常驻来源的关键 ID 统一收拢到 `RewardSchedule`；小游戏与兑换码的普通圆点领取逻辑合并。
- 结果：不改变现有 UI、固定顺序、倒计时、垫抽数、历史记录或库存逻辑。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 发布前结构复查

- 范围：模块配置别名、进度类型分流、普通圆点领取逻辑和后续开发说明。
- 结果：移除重复配置别名，统一从 `ProgressModuleDefinition` 读取模块参数。
- 结果：新增项目级 `AGENTS.md`，明确新增模块步骤、UI 锁定边界和验证命令。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 手动更新按钮颜色统一

- 范围：常驻奖励、监察密令和活动小游戏的手动更新按钮。
- 结果：抽取共享 `ManualResetButton`，统一使用未完成态的 `WidgetPalette.accent` 颜色，保持尺寸和位置不变。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-14 统一配置与颜色审计

- 范围：抽卡规划颜色引用、设置页颜色引用和日/周/月标准奖励配置。
- 结果：重复颜色统一从 `WidgetPalette` 读取，手动更新按钮继续共用 `ManualResetButton`。
- 结果：日、周、月标准奖励统一使用 `RewardSourceDefinition`，不再使用匿名 tuple。
- 结果：未改变现有颜色值、布局、奖励数值、领取键或页面顺序。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-15 独立应用发布检查

- 范围：Swift Package 独立构建、`.app` 打包入口、README 和 CHANGELOG。
- 结果：`Package.swift`、`script/assemble_app.sh` 和 `build-release-app.sh` 已接入，可生成并打开不依赖 VS Code 的 `dist/PTNHypercubeWidget.app`。
- 结果：修复 Swift 6 release 构建中的静态偏好键声明，`Info.plist` 校验和可执行文件检查通过。
- 结果：`git diff --check` 通过；保留现有 UI、奖励逻辑和存档键不变。
- 未解决：构建仍有现有 Swift 并发提示，但不影响 release 打包。

### 2026-08-15 UP 弹窗外部收起

- 范围：UP 尖号弹窗的外部点击收起行为。
- 结果：收起点击层移到主内容上方、UP 弹窗下方，点击弹窗外可自动关闭，弹窗内部保持可用。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-15 UP 数字命中区域

- 范围：UP 数字、数字与尖号之间以及尖号的点击区域。
- 结果：数字和尖号合并为同一可点击矩形，宽度随 UP 数位自动伸缩，弹窗锚点和 UI 尺寸保持不变。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-15 顶部数字基线

- 范围：顶部总抽数与 UP 数的数字对齐。
- 结果：以 UP 数字为基准，将总抽数数值视觉下移 3pt，保持标题和其他布局不变。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-15 主线签到

- 范围：双活动池期间的主线签到自动周期、三阶段解锁和领取状态。
- 结果：复用 `SecretPassProgress`、`makeProgress` 和圆点 UI；第 9、11、13 天分别提供 60 异方晶，后两项需前一项已领取后才解锁。
- 结果：活动池结束后自动隐藏，下一周期按活动池重新生成；`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-15 主线签到第九天解锁

- 范围：主线签到首个圆点的日期解锁判断。
- 结果：改为按奖励日判断，不再要求等到活动池的具体开服时刻；第九天当天即可点击第一个圆点。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-15 周年庆周期

- 范围：限定池周年庆签到、兑换码数量和问卷调查周期。
- 结果：周年庆签到复用主线签到的进度函数和圆点 UI；限定池归入特殊双奖励周期，兑换码生成 3 个奖励位，问卷调查跟随该周期。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-15 服从度奖励

- 范围：服从度橙、紫、蓝六个圆点、即时领取/取消和手动刷新。
- 结果：复用审查的 `DailyProgress` 领取逻辑，橙色为 0/40 对应 60/30，紫色为 0/40 对应 20/10，蓝色为 0/40 对应 10/5。
- 结果：圆点点击立即更新库存和历史，再次点击可取消；手动刷新后统一开始新一轮，审查刷新时保留原有完成奖励结算。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-15 审查完成按钮（已替换）

- 范围：审查完成按钮的可点击状态和按钮复用。
- 结果：审查未达到 4/3 次时保留禁用按钮，达到上限后启用；高级、高级已购和完成按钮统一使用 `RewardActionButton`。
- 结果：`git diff --check` 和 `./build-standalone-app.sh` 均通过。
- 未解决：无已知代码问题。

### 2026-08-15 审查奖励优化

- 范围：审查橙色和紫色阶段奖励，以及完成后的额外奖励。
- 结果：橙色为 80/80/50/80，紫色为 60/50/60；取消完成额外奖励，圆点点击仍即时领取/取消，手动刷新继续换轮。
- 结果：`git diff --check` 通过，`./build-standalone-app.sh` 已生成可执行的 `dist/PTNHypercubeWidget.app`；系统自动打开阶段返回 `kLSNoExecutableErr`。
- 未解决：应用自动启动仍需在本机 Finder 或终端手动打开验证。

### 2026-08-15 进度模块上下布局

- 结果：派遣、审查、服从度改为参考密令的上下布局，标题和重置入口在上方，圆点在下方。
- 检查：`git diff --check` 通过；已生成 `dist/PTNHypercubeWidget.app`，自动打开仍返回 `kLSNoExecutableErr`。

### 2026-08-15 独立启动修复

- 结果：独立构建脚本改为直接启动新生成的可执行文件，避免打开旧实例。
- 检查：`git diff --check` 通过，`./build-standalone-app.sh` 成功生成并启动新 App。

### 2026-08-15 额外记录排序与签到显示

- 结果：未完成的进度模块移到额外记录顶部并保持原顺序；主线签到第九天解锁，周期结束后隐藏。
- 检查：`git diff --check` 通过，`./build-standalone-app.sh` 成功生成并启动新 App。

### 2026-08-15 基地监管自动生产

- 结果：基地监管按固定周期自动增加 4 异方晶并同步今日统计与历史，自动记录不参与撤销；模块固定置顶并复用统一圆圈组件。
- 检查：`git diff --check`、独立构建、Swift 6 发布构建和自动启动均通过。

### 2026-08-15 更新日志分类

- 结果：全新功能归入“新增”，既有功能改动归入“优化”，条目只保留用户可见功能并排除 UI 调整。

### 2026-08-16 每日情绪检测

- 结果：额外记录新增每日情绪检测 30 异方晶，复用监管事件的奖励日、领取、取消和历史逻辑。
- 结果：每日额外、每周和每月简单奖励统一复用配置生成器；暗域、试用、数据间隙等特殊规则统一复用奖励条目工厂。
- 检查：`git diff --check`、独立构建、Swift 6 发布构建和自动启动均通过。

### 2026-08-16 圆形控件绘制约定

- 结果：编辑按钮仅将内部图标左移 1 点，外圆尺寸、位置和命中区域保持不变。
- 约定：圆形控件的外圈和内部图标必须分层绘制，后续图标微调不得影响外圈。

### 2026-08-16 卡池数据来源

- 结果：README 与开发约定固定记录 S1N Banners 来源，后续无需重复提供链接。
- 检查：网站使用公开只读 `timeline` 数据提供卡池类型、角色、起止时间和确认状态，当前远端记录与本地卡池配置一致。
- 结论：可实现自动获取，但必须使用远端校验、本地兜底和稳定 ID 映射，不能直接覆盖现有配置与存档键。

### 2026-08-16 兑换码弹窗

- 范围：兑换码标题尖号、顶层弹窗、点击外部收起、逐条复制和复制反馈。
- 数据：读取 S1N 当前有效的纯英文 Gift Code，远端失败时使用缓存或带有效期的内置兑换码。
- 数量：主线和周年庆显示 3 条，普通活动显示 1 条；远端逐日新增时按开始时间依次补入，当前第 4 条不进入 3 奖励位周期。
- 刷新：仅在活动池或限定池开池日前后 7 天，于德国时间 08:00、16:00 检查，失败后保留缓存并允许重试。
- 回归：兑换码圆点、奖励、倒计时、领取键和固定顺序保持不变。

### 2026-08-16 卡池增量同步

- 数据：德国时间每日 08:00、16:00 读取 S1N 已确认卡池，接口失败后保留缓存并允许重试。
- 追加：只加入按卡池类型和橙色角色名判断为本地不存在的记录，并辅以 S1N 来源 ID 去重，不覆盖或修正已有卡池日期。
- 映射：活动、复刻、定轨、统合和限定池复用现有类型、选择方式与垫抽分组。
- 回归：本地卡池 ID、手动时间、角色、类型、选择状态和垫抽数保持不变。

### 2026-08-16 弹窗透明度

- 结果：UP 列表和兑换码列表使用半透明玻璃背景，卡池选择弹窗使用更高不透明度避免底层内容透出。
- 回归：弹窗尺寸、位置、圆角和交互保持不变。

### 2026-08-16 弹窗层级统一

- 结果：UP、兑换码和卡池选择统一使用最高弹窗层级，展开中的卡池不会再被后续卡片覆盖。
- 回归：一次只展开一个卡池，弹窗不参与卡片高度计算，尺寸、位置和样式保持不变。
- 检查：`git diff --check`、Swift 编译和 `./build-standalone-app.sh` 均通过。

### 2026-08-16 弹窗尖号命中范围

- 结果：UP 使用 `24×24` 汇总尖号命中范围，兑换码和卡池选择使用 `30×30` 模块尖号命中范围。
- 回归：UP 数字与尖号之间仍可点击，兑换码和卡池尖号的弹窗锚点保持不变。

### 2026-08-16 历史名称迁移

- 结果：新旧每日奖励历史统一显示为“每日监察任务”，旧记录保留原 ID、时间、奖励和领取键。
- 结果：派遣显示具体异方晶档位；审查按颜色显示狂级/危级禁闭者第一至第四阶段；服从度显示狂级、危级、普级禁闭者的获得或 40% 奖励。
- 迁移：旧服从度名称按领取键重建，废弃的审查完成奖励从历史、领取状态和库存中一次性清理。
- 回归：历史撤销继续按原领取键恢复任务与库存。

### 2026-08-16 抽卡消耗记录

- 入口：每个卡池显示“记录”按钮，仅绿色完成态可点击，弹窗复用统一顶层容器。
- 数据：记录弹窗保存赠送票、蓝票、UP 数和 UP 总数；确认时保存 A，抽数按 A 减当前垫抽自动计算，并按池型汇总。
- 校验：蓝票记录不能超过蓝票、可折算异方晶与该卡池已扣库存的合计；空白输入按 0 处理。
- 回归：只记录赠送票时蓝票和总抽数不变，重新编辑不会重复扣除蓝票。

### 2026-08-16 逻辑整理与回归

- 范围：抽卡记录库存扣减、历史生成与撤销，以及 S1N 卡池和兑换码请求逻辑。
- 结果：赠送票不扣库存，蓝票不足部分正确折算异方晶，撤销后库存与卡池记录完整恢复；S1N 请求统一复用共享入口。
- 检查：`git diff --check`、warnings-as-errors 类型检查及隔离 UserDefaults 回归测试通过。
